# Copy to terraform.tfvars and fill in secrets (never commit terraform.tfvars)

resource_group_name = "rg-margem-prod"
location            = "westeurope"
environment_name    = "prod"

postgres_admin_login    = "margemadmin"
postgres_admin_password = "CHANGE_ME_STRONG_PASSWORD"
jwt_secret_key          = "CHANGE_ME_MIN_32_CHAR_JWT_SECRET_KEY"

# After building/pushing to ACR, set e.g. margemregistry.azurecr.io/margem-api:1.0.0
api_image = "margemregistry.azurecr.io/margem-api:1.0.0"

create_container_registry = true
acr_name                  = "margemregistry"

min_replicas = 1
enable_key_vault_purge_protection = true

# Set to your Container App FQDN after first deploy, e.g. ["margem-prod-api.xxx.azurecontainerapps.io"]
allowed_hosts = "[\"margem-prod-api.azurecontainerapps.io\"]"

# Restrict to your mobile app / web origins — no wildcard in production
cors_origins = "[\"https://margem.app\"]"
