[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    no_db: skip database fixture for pure unit tests
filterwarnings =
    ignore:datetime.datetime.utcnow\(\) is deprecated:DeprecationWarning
