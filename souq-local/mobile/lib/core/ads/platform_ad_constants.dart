/// Platform display-ad placement identifiers (must match backend AD_PLACEMENTS).
class PlatformAdPlacements {
  PlatformAdPlacements._();

  static const fullPage = 'full_page';
  static const mobilePlatform = 'mobile';
}
